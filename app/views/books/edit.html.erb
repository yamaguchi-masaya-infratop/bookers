<h1>編集画面</h1>
<%= form_with model: @book, url:update_book_path(@book.id), method: :patch do |f| %>
    <h4>本のタイトル</h4>
    <%= f.text_field :title %>
    
    <h4>感想</h4>
    <%= f.text_a :body %>
    
    <%= f.submit '保存' %>
<% end %>